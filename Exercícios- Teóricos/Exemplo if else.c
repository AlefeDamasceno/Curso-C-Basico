#include <stdio.h>

int num = 0;

int main(){
    if (num > 0){
        printf("Numero Positivo!");
    }else if (num < 0){
        printf("Numero Negativo!");
    }else{
        printf("Numero 0!");
    }
}
